/**
 * @module grafanaAPI Router
 * @description Routes all requests to grafana API
 */

import { Router, Request, Response } from 'express';

const router = Router();

// get datasource info from grafana
router.get()

// POST dashboard to grafana
router.post()

export default router